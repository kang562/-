# 桌面电子宠物开发计划（Windows MVP）

## 1. 目标与范围

本项目开发一个运行在 Windows 桌面的电子宠物。它以透明、无边框、可置顶的小窗口呈现，可替换 PNG 宠物素材，并将键盘或鼠标操作以短暂气泡显示在宠物上方。

首版只保证 Windows 10/11 可用。macOS、Linux（尤其 Wayland）因全局输入监听权限与能力不同，不包含在 MVP 验收范围内。

### 1.1 MVP 必须具备

- 透明、无边框、可置顶的宠物窗口。
- 宠物可拖拽移动，启动时位于主屏幕右下角。
- 可从配置中选择一套 PNG 素材；缺少素材时显示清晰的占位状态。
- 显示待机动画，支持静态图或 PNG 序列帧。
- 监听键盘按下、鼠标左右键和滚轮，并在宠物上方显示 1.5 至 2 秒的提示气泡。
- 提供托盘菜单：显示/隐藏、置顶开关、切换皮肤、暂停输入显示、退出。
- 退出时停止所有后台监听和计时器。
- 可打包为无需安装 Python 的 Windows 可执行文件。

### 1.2 MVP 不包含

- 录制、保存、上传或分析用户输入内容。
- 语音、联网、账号、商城、AI 对话、Live2D。
- macOS/Linux 正式支持。
- 任意应用内完整按键记录；安全输入场景只做最小化展示或允许用户暂停。

## 2. 技术决策

| 领域 | 选择 | 原因 |
| --- | --- | --- |
| 语言 | Python 3.12 | 开发快、生态足够、适合首版。 |
| GUI | PySide6 | Qt 官方绑定采用 LGPL，透明窗口、计时器、托盘与信号机制成熟。 |
| 全局输入 | pynput | 可在 Windows 中监听键鼠，接口简单。 |
| 图像 | Qt `QPixmap` / `QMovie` | 优先复用 Qt 渲染；Pillow 仅用于预处理或素材检查。 |
| 配置 | JSON | 人和程序都易编辑，首版无需数据库。 |
| 打包 | PyInstaller | 生成独立 Windows exe，适合内部测试和分发。 |
| 测试 | pytest + 手工验收 | 逻辑使用单元测试，窗口与全局输入使用人工验证。 |

不选择 Electron 的原因是打包体积和常驻内存偏大；不选择 C++ Qt 的原因是首版不需要该级别的性能与工程复杂度。

## 3. 环境准备

### 3.1 开发环境

- 操作系统：Windows 10 或 Windows 11，64 位。
- Python：3.12.x，安装时勾选 `Add Python to PATH`。
- 编辑器：VS Code，安装 Python 扩展。
- 版本控制：Git。
- 终端：PowerShell。

确认环境：

```powershell
python --version
git --version
```

### 3.2 创建虚拟环境与依赖

在项目根目录执行：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install pyside6 pynput pillow pyinstaller pytest
pip freeze > requirements.txt
```

若 PowerShell 阻止激活脚本，仅对当前终端执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

不在管理员身份下运行应用作为常规方案。普通权限程序无法可靠监听以管理员身份运行的软件，这是 Windows 的隔离机制；只有在明确需求和安全评估后才考虑提升权限。

### 3.3 建议目录

```text
桌宠/
  app/
    __init__.py
    main.py                 # 应用入口
    pet_window.py           # 透明窗口、拖拽、气泡绘制
    input_listener.py       # pynput 监听与 Qt 信号转换
    animation.py            # 静态图/序列帧动画控制
    config.py               # 配置读取、默认值和校验
    tray.py                 # 系统托盘菜单
    models.py               # 小型数据模型和事件类型
  assets/
    skins/
      default/
        manifest.json
        idle/
        typing/
        click/
  config/
    settings.json
  tests/
    test_config.py
    test_event_formatter.py
  requirements.txt
  pyproject.toml
  README.md
```

每套皮肤使用独立目录，禁止将素材路径硬编码到 Python 源码。

## 4. 配置与素材约定

`config/settings.json` 初始示例：

```json
{
  "skin": "default",
  "always_on_top": true,
  "input_display_enabled": true,
  "bubble_duration_ms": 1800,
  "window_scale": 1.0,
  "position": { "x": null, "y": null }
}
```

`assets/skins/default/manifest.json` 初始示例：

```json
{
  "name": "默认宠物",
  "canvas": { "width": 320, "height": 400 },
  "idle": ["idle/001.png", "idle/002.png"],
  "typing": ["typing/001.png"],
  "click": ["click/001.png"]
}
```

素材规则：

- 使用带 Alpha 通道的 PNG，尺寸按皮肤 `canvas` 保持一致。
- 序列帧按三位或四位数字排序，例如 `001.png`。
- 气泡不写入宠物图片，由程序绘制，以适应不同按键文本。
- 所有第三方素材必须记录来源和使用许可。

## 5. 架构与关键约束

### 5.1 事件流

```text
pynput 后台监听线程
  -> InputEvent（按键/鼠标、时间、展示文本）
  -> Qt Signal
  -> 主线程 PetWindow
  -> 气泡与动作状态更新
```

`pynput` 回调不能直接修改 Qt 控件。监听模块必须发出 Qt 信号，由 GUI 主线程更新窗口，避免随机崩溃和界面卡顿。

### 5.2 窗口行为

- 使用无边框、透明背景、置顶窗口标志。
- 应用显示时不主动抢占键盘焦点；Windows 下视情况补充“显示但不激活”的原生窗口属性。
- 宠物本体区域允许拖拽和右键菜单；后续若增加“鼠标穿透”，必须提供托盘菜单开关，否则用户无法操作宠物。
- 置顶关闭、缩放、皮肤和位置变更后立即写入配置。

### 5.3 输入显示与隐私

- 默认只显示单个输入事件，例如 `A`、`Space`、`鼠标左键`、`滚轮上`。
- 不保存原始按键、坐标、时间线或应用名称；日志中同样不得写入输入值。
- 提供“暂停输入显示”托盘开关，并在关闭时停止向 UI 分发事件。
- 发布说明明确告知应用使用全局输入监听；此能力可能被安全软件提示。
- 后续若支持文本输入，默认显示为通用提示，例如“正在输入”，而非拼接完整文字。

### 5.4 平台限制

- Windows：普通权限下可覆盖绝大多数桌面应用；无法可靠捕获管理员应用中的输入。
- macOS：需要用户在系统设置中授予辅助功能/输入监控许可。
- Linux X11：通常可实现；Wayland：全局监听经常被桌面环境限制。

因此，跨平台扩展时应为每个平台单独验证监听能力，不能只依赖 `pynput` 的接口一致性。

## 6. 分阶段实施步骤

### 阶段 0：项目骨架

1. 创建第 3.3 节目录、虚拟环境、依赖清单和 `.gitignore`。
2. 添加 `pyproject.toml`，配置 pytest 与代码格式化工具。
3. 创建最小 `main.py`，启动 Qt 应用并输出版本信息。

验收：`python -m app.main` 可启动并干净退出；`pytest` 可执行。

### 阶段 1：宠物窗口与素材加载

1. 实现 `PetWindow`：透明、无边框、默认置顶、初始右下角。
2. 读取 `settings.json` 与皮肤 `manifest.json`。
3. 加载静态 PNG；素材或配置无效时显示诊断占位图和可读错误日志。
4. 实现左键拖拽、右键菜单和位置持久化。

验收：替换皮肤目录后重启即可切换外观；宠物拖动后下次启动恢复位置。

### 阶段 2：动画与状态

1. 实现定时帧动画器，支持循环待机和一次性动作。
2. 定义状态优先级：`click` / `typing` 高于 `idle`，状态结束后回到 `idle`。
3. 处理帧缺失、不同图片尺寸与高 DPI 缩放。

验收：待机动画稳定运行 30 分钟，无明显内存持续增长；连续切换动作不会闪烁或停帧。

### 阶段 3：键鼠监听与提示气泡

1. 将 `pynput` 包装为独立监听模块，并定义 `InputEvent`。
2. 将按键规范化为可读名称：字母、数字、功能键、空格、方向键、鼠标左右键、中键、滚轮方向。
3. 通过 Qt 信号投递给主线程；以单个 `QTimer` 管理气泡隐藏，不使用共享全局变量轮询。
4. 对连续输入进行节流：例如 80 至 150 ms 内只保留最后一个事件，避免气泡频闪。
5. 增加暂停开关，并确保关闭窗口时调用监听器 `stop()`。

验收：在记事本、浏览器和桌面切换焦点时可显示事件；高频打字不冻结窗口；退出后进程完全结束。

### 阶段 4：托盘、设置与稳定性

1. 添加托盘菜单：显示/隐藏、置顶、皮肤、输入显示、缩放、退出。
2. 增加设置加载校验与默认值回退。
3. 增加单元测试：配置解析、素材清单校验、输入格式化、节流策略。
4. 手工检查双屏、DPI 缩放、睡眠唤醒、资源缺失与重复启动。

验收：主要设置重启后保留；异常素材或配置不会导致应用崩溃。

### 阶段 5：打包与发布

1. 使用干净虚拟环境安装依赖并执行测试。
2. 生成单文件 Windows 包：

```powershell
pyinstaller --noconfirm --clean --onefile --windowed --name "桌面宠物" --add-data "assets;assets" --add-data "config;config" app/main.py
```

3. 在未安装 Python 的 Windows 测试机验证运行、托盘、输入显示和退出。
4. 输出版本号、许可证、素材署名、隐私说明和已知限制。

验收：从 `dist/桌面宠物.exe` 启动后不依赖开发目录或 Python 环境；将该 exe 复制到另一台 Windows 10/11 电脑后可直接运行。用户设置写入 `%APPDATA%/DesktopPet`，不写入 exe 所在目录。

## 7. 测试清单

每次功能完成后至少检查：

- [ ] 冷启动、退出、重新启动均正常。
- [ ] 窗口透明、无标题栏、可拖动且不遮挡主要操作区域。
- [ ] 置顶开关、显示/隐藏、缩放和皮肤切换正常。
- [ ] 键盘、鼠标左右键、中键、滚轮显示正确。
- [ ] 暂停输入显示后不再更新气泡。
- [ ] 素材丢失、JSON 格式错误、无效路径有可读提示且不会崩溃。
- [ ] 30 分钟待机与高频输入期间，CPU 和内存没有明显持续增长。
- [ ] 退出后任务管理器中没有残留 Python 或宠物进程。

## 8. 已有原型的处理建议

现有 `新建 文本文档.txt` 仅作为参考，不直接扩展。重建时应处理以下问题：

- `QAction` 在 PyQt6 中应从 `PyQt6.QtGui` 导入；若切换到 PySide6，也应使用相应的 QtGui 导入。
- 原型定义了 `timerEvent`，但没有启动基础计时器，键鼠提示不会持续刷新。
- 监听结果使用全局变量和 UI 轮询；正式实现改为“监听线程 -> Qt 信号 -> GUI 主线程”。
- 未在关闭时停止 `pynput` 监听器，可能造成退出残留。
- 图片路径、尺寸、文案等均应迁移到配置与皮肤清单中。

## 9. 后续变更规则

后续开发和需求修改以本文件为基线。每项变更应在提交前记录：目的、影响的模块、配置/素材兼容性、测试方式和回滚方式。

- 新功能先判断是否属于 MVP；不属于的功能进入“后续版本”，避免阻塞首版。
- 改动全局监听、权限、数据记录或联网能力时，必须重新评估隐私说明和安全风险。
- 改动皮肤清单格式时，提供迁移策略或保持旧格式兼容。
- 新增平台支持前，先完成该平台上的最小监听与透明窗口验证，再承诺功能范围。
- 不得为了方便调试而写入完整键盘输入、鼠标轨迹或用户隐私数据。

## 10. 下一步

按“阶段 0 -> 阶段 1 -> 阶段 3”的顺序推进。阶段 2 可在静态宠物与输入气泡已经可用后加入，避免动画阻塞核心交互验证。
