# Windows 桌面电子宠物使用与打包说明

## 交付目标

开发完成后，交付文件是单个 `dist\\桌面宠物.exe`。把它复制到任意 Windows 10/11 64 位电脑并双击运行即可；目标电脑不需要安装 Python、pip 或本项目源码。

单文件 exe 的第一次启动会短暂解压 Qt 运行库到系统临时目录，这是 PyInstaller 的正常行为，不需要用户手动处理。

## 开发环境与依赖

1. 安装 Windows 版 Python 3.12（安装时勾选 `Add Python to PATH`）。
2. 在项目根目录打开 PowerShell，依次执行：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

也可以双击 `安装依赖.bat` 完成同样操作。

依赖说明：

- `PyQt6`：透明无边框窗口、菜单与气泡绘制。
- `pynput`：Windows 全局键盘、鼠标钩子。
- `Pillow`：PNG 完整性验证。
- `PyInstaller`：将 Python 和全部依赖合并为 exe。

## 启动与操作

开发时双击 `运行桌宠.bat`，或执行：

```powershell
.\.venv\Scripts\pythonw.exe .\桌面宠物.py
```

- 左键按住宠物拖动位置；单击宠物会随机显示台词。
- 每 12 秒会显示一条随机角色台词。
- 在任意普通应用窗口中按键、点击或滚动时，宠物头顶显示最新事件，1.8 秒后隐藏。
- 右键宠物可放大、缩小、切换置顶或退出。

## 自定义素材与台词

### PNG 素材

素材路径固定为：

```text
assets/
  sandrone_q.png
```

推荐要求：

- 使用透明背景的 PNG，建议画布 `320 x 400` 或同等纵向比例。
- 保留人物完整头部和下摆，四周留出 8 到 16 像素透明边缘，避免气泡或窗口边缘裁切。
- 程序使用等比缩放，图片不会被拉伸。
- PNG 缺失、格式损坏或无透明通道时，程序自动显示内置的灰发蓝眼、黑红金礼服 Q 版兜底形象，不会崩溃。

替换贴图后，重新运行打包即可把新素材放进 exe。台词、气泡时间、窗口尺寸和图片路径都集中在 `桌面宠物.py` 顶部的“关键配置区”。

## 打包成单文件绿色 exe

确认开发运行正常后，在项目根目录执行：

```powershell
.\.venv\Scripts\pyinstaller.exe --noconfirm --clean --onefile --windowed --name "桌面宠物" --add-data "assets;assets" --hidden-import pynput.keyboard._win32 --hidden-import pynput.mouse._win32 "桌面宠物.py"
```

也可以双击 `打包Windows版.bat`。参数说明：

- `--onefile`：生成单个 exe。
- `--windowed`：运行时不显示黑色控制台窗口。
- `--add-data "assets;assets"`：把 PNG 素材加入 exe。
- 两个 `--hidden-import`：确保 Windows 键鼠监听后端被 PyInstaller 收集。

打包后的开发目录会出现：

```text
build/                 # 打包临时文件，可删除
dist/
  桌面宠物.exe          # 唯一需要分发给其他用户的文件
桌面宠物.spec           # PyInstaller 构建描述，可保留或删除
```

实际对外软件包只需要：

```text
桌面宠物.exe
```

## 常见问题

### 1. `ModuleNotFoundError: No module named 'PyQt6'`

没有在正确的虚拟环境中安装依赖。运行 `安装依赖.bat`，或激活 `.venv` 后执行 `pip install -r requirements.txt`。

### 2. 键盘或鼠标没有显示提示

确认 `pynput` 已安装；普通权限程序不能监听以管理员身份运行的软件。部分游戏的反作弊或独占输入模式会阻止任何普通桌面程序监听输入，这是系统或游戏保护机制，不能绕过。

### 3. Windows Defender / SmartScreen 提示未知发布者

PyInstaller 生成的个人 exe 默认没有代码签名，可能出现该提示。对外正式发布时，应购买并使用 Windows 代码签名证书签名；不要通过关闭安全软件来解决。

### 4. exe 在其他电脑上无法启动

请确认目标是 Windows 10/11 64 位，并从 `dist\\桌面宠物.exe` 复制文件。重新在干净环境中打包，确认没有漏掉 `assets` 参数。必要时在目标电脑查看 Defender 隔离历史。

### 5. 自定义图片不显示

检查文件名是否精确为 `sandrone_q.png`，位置是否在 `assets` 目录，且确实是可打开的 PNG。程序会回退到内置形象，说明主程序仍可正常工作。

## 隐私与限制

程序只将最新一个键鼠事件显示到屏幕气泡，不写入文件、不上传网络、不保留按键历史。全局监听会受到 Windows 权限边界、游戏反作弊和独占输入模式限制；不要为绕过这些限制提升权限或规避保护机制。
